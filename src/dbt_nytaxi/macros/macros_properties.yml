version: 2

macros:
  - name: get_payment_type_description
    description: This macro receives a payment_type and returns the corresponding description.
    arguments:
      - name: payment_type
        type: int
        description: payment_type value must be one of the accepted values, otherwise the macro will return null
  - name: get_rate_code_description
    description: This macro receives a RatecodeID and returns the corresponding description.
    arguments:
      - name: RatecodeID
        type: int
        description: RatecodeID value must be one of the accepted values, otherwise the macro will return null
  - name: get_trip_type_description
    description: This macro receives a trip_type and returns the corresponding description.
    arguments:
      - name: trip_type
        type: int
        description: trip_type value must be one of the accepted values, otherwise the macro will return null